<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.EBookApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.EBookApp" parent="Base.Theme.EBookApp" />

    <style name="SearchViewTheme" parent="ThemeOverlay.AppCompat.Light">
        <item name="colorControlNormal">@color/black</item> <!-- icon color -->
        <item name="colorControlActivated">@color/black</item>
    </style>
</resources>